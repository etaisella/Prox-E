Evaluate whether your edit correctly addresses the original instruction.

Respond in ONE of two formats:

## If the edit is CORRECT:
```
STATUS: DONE
The edit successfully addresses the instruction because [brief explanation].
```

## If the edit needs FIXING:
```
STATUS: NEEDS_FIX
```

### Problem
What is wrong with the current edit.

### Fix Plan
How you will fix it.

### Updated JSON
```json
[
  {"index": 0, "scale": [...], "translation": [...], "rotation": [...], "exponents": [...], "color": [...]},
  ...
]
```
